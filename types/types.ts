export interface SkillInterface {
	title: string;
	path: string;
	_id?: string;
}
export interface ProjectInterface {
	title: string;
	tag: string;
	description: string;
	image: string;
	category: string;
	_id?: string;
}
